package Chapter7_Exercises;

import Chapter7.Position;

/**
 * Suppose we want to extend the PositionalList abstract data type with a
 * method, indexOf(p), that returns the current index of the element stored at
 * position p. Show how to implement this method using only other methods of the
 * Positional- List interface (not details of our LinkedPositionalList
 * implementation).
 */
public class R_7_12 {
	// int indexOf(Position<E> p);
}
