package Chapter7_Exercises;

import Chapter7.Position;

/**
 * Suppose we want to extend the PositionalList abstract data type with a
 * method, findPosition(e), that returns the first position containing an
 * element equal to e (or null if no such position exists). Show how to
 * implement this method using only existing methods of the PositionalList
 * interface (not details of our LinkedPositionalList implementation).
 */
public class R_7_13 {
	// Position<E> findPosition(E e);
}
